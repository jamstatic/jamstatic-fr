      {%- if page.paginator.pages is defined -%}
        {%- set paginator = page.paginator -%}
        {%- set paginator_current = paginator.current -%}
        {%- set paginator_count = paginator.count ~%}
        <div class="flex flex-row items-center text-base md:text-lg font-semibold text-violet dark:text-blue-500">
          {%- if paginator.links.prev is defined ~%}
          <div>&larr;&nbsp;</div><a class="w-32 flex-auto text-left hover:underline dark:hover:text-blue-300" href="{{ url(site.page(paginator.links.prev)) }}">
            {%- trans %}Précédent{% endtrans -%}
          </a>
          {%- else ~%}
          <div class="w-32 flex-auto text-left"></div>
          {%- endif ~%}
          <div class="w-64 flex-auto text-center">
            {%- trans %}%paginator_current% sur %paginator_count%{% endtrans -%}
          </div>
          {%- if paginator.links.next is defined ~%}
          <a class="w-32 flex-auto text-right hover:underline dark:hover:text-blue-300" href="{{ url(site.page(paginator.links.next)) }}">
            {%- trans %}Suivant{% endtrans -%}
          </a><div>&nbsp;&rarr;</div>
          {%- else ~%}
          <div class="w-32 flex-auto text-right"></div>
          {%- endif ~%}
        </div>
      {%- endif ~%}